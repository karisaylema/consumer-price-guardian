AWS_REGION=us-east-1
# Some regions require the cross-region inference-profile id instead,
# e.g. us.anthropic.claude-opus-5
BEDROCK_MODEL_ID=anthropic.claude-opus-5

# Filled in after `terraform apply` (see infra/outputs.tf)
RAW_DATA_BUCKET=
PROCESSED_DATA_BUCKET=
GLUE_DATABASE_NAME=
ATHENA_WORKGROUP=
ATHENA_OUTPUT_LOCATION=
OPENSEARCH_COLLECTION_ENDPOINT=
OPENSEARCH_INDEX_NAME=consumer-law-text
